name: async_callback_test_integration
description: A simple command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.12.0 <3.0.0'

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  pedantic: ^1.10.0
  test: ^1.17.11
dependencies: 
  isolate_rpc:
    git:
      url: https://github.com/leastauthority/isolate_rpc
      ref: dev-shareef
